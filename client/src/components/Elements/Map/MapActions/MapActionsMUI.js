import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  mapActions: {
    display: 'flex',
    flexDirection: 'row',
    columnGap: '8px',
  },
}));

export { useStyles };
