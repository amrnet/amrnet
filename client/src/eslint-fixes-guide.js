/* eslint-disable no-unused-vars */
/* eslint-disable prefer-const */
/* eslint-disable no-undef */

// This file contains ESLint disable comments for complex cases
// that require careful manual review before fixing

// Use this as a reference when fixing ESLint issues:

// 1. For unused parameters in function signatures, prefix with underscore:
//    function myFunction(usedParam, unusedParam) => function myFunction(usedParam, _unusedParam)

// 2. For unused imports that might be needed later, comment them out:
//    import { UnusedComponent } from './module'; => // import { UnusedComponent } from './module';

// 3. For variables that should be const instead of let:
//    let unchangedVar = value; => const unchangedVar = value;

// 4. For duplicate imports, combine them:
//    import { A } from './module';
//    import { B } from './module';
//    =>
//    import { A, B } from './module';

// 5. For unused variables in destructuring, use underscore prefix:
//    const { used, unused } = object; => const { used, _unused } = object;

export default {};
